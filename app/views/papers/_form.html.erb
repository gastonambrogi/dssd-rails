<%= simple_form_for(@paper) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  <div class="row">
  	<%= f.input :title, label: 'Titulo' %> </div>
  	<%= f.input :summary, label: 'Descripción', as: :text, input_html: { maxlength: 200} %>
  <div class="row">
  	<%= f.input :theme, prompt: 'Tema', collection: ['Cloud', 'WS', 'BPM', 'SOA'], as: :radio_buttons %>
  </div>	<%= f.input :personal_email, label: 'Email personal', as: :email %>
  	<%= f.input :email, label: 'Email de Google', as: :email %>
  	<%= f.input :presentation, prompt: 'Tipo de presentacion', collection: ['Poster', 'Conferencia'], as: :radio_buttons %>
	<%= f.association :users, label: 'Autores secundarios', collection: @authors, prompt: "Elige los autores secundarios." %>
  
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
